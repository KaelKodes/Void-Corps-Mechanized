# Post-apoc kitbash (parked)

Free Building_12 cluster from **Oleg Ushenok** (CGTrader / Fab / ArtStation).

**Not used in live arenas** — pulled until we have enough authored props for a consistent look.
See repo root `BUY_LIST.md` for the full city kit.

Files stay here for a later re-wire (`12.fbx` + PBR PNGs + author `Read me.txt`).
